services:
  data-engineering:
    container_name: data-engineering
    build: ./data_engineering
    expose:
      - "5001"
    depends_on:
      - database
      - backend
      - frontend

  database:
    container_name: db
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: signalements_utilisateurs
    expose:
      - "5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/signalements.sql:/docker-entrypoint-initdb.d/signalements.sql

  backend:
    container_name: backend
    build: ./backend
    expose:
      - "3001"
    depends_on:
      - database

  frontend:
    container_name: frontend
    build: ./frontend
    expose:
      - "3000"
    depends_on:
      - backend

  nginx:
    image: nginx:latest
    container_name: nginx
    depends_on:
      - backend
      - frontend
      - data-engineering
    ports:
      - "1234:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
<<<<<<< HEAD
=======

volumes:
  postgres-data:
>>>>>>> 540a1ae6a01cdb52e6b7ec4cf659f0d13beae21b
